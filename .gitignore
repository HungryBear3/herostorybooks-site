node_modules
.next
.env.production
.env.local
.env.rebuild

.vercel

# OS / editor
.DS_Store
**/.DS_Store

# Build/cache
tsconfig.tsbuildinfo

# Local-only artifacts and rescue scratch space
.data/
tmp/

# Local sample/QA prompt packs (regenerable, not source)
fal_sample_prompt_pack_*.json
FAL_SAMPLE_PROMPT_PACK_*.md
tmp_*.jsonl

# Knowledge-graph output (regenerated from source)
graphify-out/

# Local visual QA artifacts (regenerated by npm run visual:snapshots)
visual-reports/

# Local sample-art review scratch space (not deploy/repo source)
public/assets/backups-*/
public/assets/candidates-*/

# Local agent workspace context, not app source
.openclaw/
AGENTS.md
HEARTBEAT.md
IDENTITY.md
SOUL.md
TOOLS.md
USER.md
