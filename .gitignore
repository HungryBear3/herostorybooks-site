node_modules
.next
.env.production
.env.local
.env.rebuild

.vercel

# OS / editor
.DS_Store
**/.DS_Store

# Build/cache
tsconfig.tsbuildinfo

# Local-only artifacts and rescue scratch space
.data/
tmp/
.hsb-email-ledger/

# Local sample/QA prompt packs (regenerable, not source)
fal_sample_prompt_pack_*.json
FAL_SAMPLE_PROMPT_PACK_*.md
tmp_*.jsonl

# Knowledge-graph output (regenerated from source)
graphify-out/

# Order-scoped local review tooling and its virtualenv. Never committed: it is
# scoped to a single real order and carries customer-identifying strings.
scripts/hsb-review-builder/
.venv-hsb-review/

# QA evidence bundles. Local artifacts only: they quote real order ids and
# server logs verbatim and must never be committed.
evidence-*/
